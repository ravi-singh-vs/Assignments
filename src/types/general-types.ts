export interface NotificationDataType {
    id: string;
    image: string;
    text: string;
    status: boolean;
    createdAt: number;
}

export interface DActiveDataType {
    id: string;
    title: string;
}